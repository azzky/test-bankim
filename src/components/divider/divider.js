import classes from './divider.module.css'
/**
 * dummy component for divider in forms etc.
 * @returns {JSX.Element}
 */
const Divider = () => (
    <hr className={classes.divider}/>
);

export default Divider;